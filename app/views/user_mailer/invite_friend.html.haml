!!! 5
%html(lang="en-US")
  %body
    %p You are invited by #{@invitation.inviter.full_name} to join MyFlix!
    %p= @invitation.message
    %p= link_to "Accept this invitation", register_with_token_url(invite_token: @invitation.invite_token)
